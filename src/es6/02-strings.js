let hello = "Hello";
let world = "world";
let epicPhrase = hello + " " + world + "!";
console.log(epicPhrase);

// template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// Multi line strings

let lorem = "esto es un string \n " + "otra linea";

let lorem2 = `esta es una prase
esta es la continuación de la frase`;
console.log(lorem)
console.log(lorem2)